package com.restaurant.controller.admin;

// THANG - Chi THANG (Admin/Manager) duoc phep truy cap
// @RestController @RequestMapping("/api/admin/users")
// @PreAuthorize("hasAnyRole('ADMIN','MANAGER')")
//
// GET    /                  → Page<UserResponse> (filter: role, status)
// GET    /{id}              → UserResponse
// POST   /                  → UserResponse (create)
// PUT    /{id}              → UserResponse (update)
// PUT    /{id}/lock         → void (lock)
// PUT    /{id}/unlock       → void (unlock)
// PUT    /{id}/reset-password → void (reset + gui email)
public class UserManagementController {
}
