# ================================================
# TEST Profile
# ================================================
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/restaurant_test
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: create-drop    # Test: tao lai moi lan run

  mail:
    host: localhost
    port: 1025
    username: test
    password: test

jwt:
  secret: TEST_SECRET_KEY_MUST_BE_AT_LEAST_256_BITS_LONG_FOR_TESTING_PURPOSES_ONLY
  expiration: 3600000
  refresh-expiration: 7200000

vnpay:
  version: 2.1.0
  command: pay
  order-type: other
  tmn-code: TEST_TMN
  hash-secret: TEST_SECRET_KEY_FOR_VNPAY_HMAC_SHA512_SIGNING
  pay-url: http://localhost:9999/paymentv2/vpcpay.html
  return-url: http://localhost:8080/api/payments/vnpay/return
  ipn-url: http://localhost:8080/api/payments/vnpay/ipn
  frontend-return-url: http://localhost:5173/payment/vnpay/return

server:
  port: 0    # Random port cho test
